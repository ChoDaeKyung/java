<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.tobi.noticeprogram.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.example.tobi.noticeprogram.model.User">
        INSERT INTO user(name, email, phone) VALUES ( #{name}, #{email}, #{phone});
    </insert>

    <select id="login" parameterType="com.example.tobi.noticeprogram.model.User">
        SELECT id, password FROM user WHERE id = #{id} and password = #{password};
    </select>

</mapper>